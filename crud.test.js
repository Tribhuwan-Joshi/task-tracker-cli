const assert = require("assert");
const { describe, test } = require("node:test");

describe("Test crud property", () => {
  test("Able to fetch tasks", () => {
    // todo or maybe skip for now
  });
});
